import React from 'react';
import {
    Container,
  } from '@material-ui/core';

const homeFeatures = () => (
    <Container>
        <h2>Heading Features</h2>
    </Container>
);

export default (homeFeatures);